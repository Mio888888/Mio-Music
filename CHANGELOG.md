# 更新日志

## 0.3.6 - 未发布

### 修复

- 修复 LX 音源的平台识别和异步初始化兼容问题，等待音源就绪后再读取音质、获取播放链接，并兼容已安装的旧版转换插件（#7）。
- Android 本地音乐支持通过系统文件选择器多选导入，复制到应用目录后建立索引；同名文件分别保存，取消导入不再提示成功（#7）。
- 改善旧版 Android WebView 的页面与播放器布局兼容性，补充视口高度回退、窄屏搜索栏适配和系统栏安全区域处理（#7）。

Android 9 真机与真实音源播放仍待复验，验证范围见 [Issue #7 修复与验证](docs/issue-7-verification.md)。

## 0.3.5 - 未发布

### 修复

- 修复 Android 本地导入 Ceru/LX 插件时，系统文件选择器返回内容 URI 导致“无效文件路径”的问题（#6）。
- 修复带引号的音质字段解析，并在选择、启动恢复及替换插件后同步实际声明的音质，避免遗漏 FLAC、Hi-Res 等选项（#6）。
- 修复插件切换、替换与取消导入时的音质同步竞态，保留仍受支持的音质偏好和内置音源。

## 0.3.4 - 2026-09-02

### 修复

- 修复 Android 端启动或手动检查更新时调用桌面更新器导致 `Unsupported OS` 错误的问题。
- 移动端更新页面改为提供 GitHub 发布页入口，方便下载最新 APK。
- 发布流程改为等待 Android APK 上传完成后再公开 Release，避免最新版本缺少安装包。
- 发布流程统一使用唯一 Release 草稿，避免并行构建产生重复草稿并遗漏 APK。

## 0.3.0 - 2026-08-24

### 优化

- 应用内更新体验重构：新增统一的 `useAppUpdater` 组合式函数，集中管理检查、下载、安装、重启的完整状态流转。
- 启动时自动检查更新：设置中的"应用启动时检查更新"开关现在真正生效。
- macOS 平台支持应用内下载并重启安装更新，不再强制跳转浏览器手动下载。

### 修复

- 修复插件安装失败提示"禁止访问本机或内网地址"的问题：插件包下载不再限制来源为本机或局域网地址（NAS、本地开发服务器等场景可用）。
